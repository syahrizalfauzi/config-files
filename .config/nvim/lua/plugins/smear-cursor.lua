return {
  "sphamba/smear-cursor.nvim",
  lazy = false,
  opts = {                         -- Default  Range
    distance_stop_animating = 1, -- 0.1      > 0
    smear_terminal_mode = true,
    time_interval = 8,
    max_length = 100
  },
}
